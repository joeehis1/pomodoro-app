export enum TimerStatus {
    Running,
    Paused,
    Stopped,
}

export enum DurationType {
    Work,
    ShortBreak,
    LongBreak,
}

export enum FontFamily {
    Kumbh = "kumbh",
    Roboto = "roboto",
    SpaceMono = "spaceMono",
}

export enum CurrentAccent {
    Orange = "accent-orange",
    Blue = "accent-blue",
    Purple = "accent-purple",
}
